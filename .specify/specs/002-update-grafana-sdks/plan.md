# Implementation Plan: Update Grafana SDKs

**Branch**: `002-update-grafana-sdks` | **Date**: 2026-02-09 | **Spec**: .specify/specs/002-update-grafana-sdks/requirements.md
**Input**: Specification from `.specify/specs/002-update-grafana-sdks/requirements.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

将 Grafana App SDK 升级到 v0.50.1、Grafana Plugin SDK Go 升级到 v0.287.0，并更新依赖记录与变更可追踪信息。实施方式为更新 Go 依赖（go.mod/go.sum）并验证标准构建与关键使用场景保持兼容。

## Current Versions & Impact Scope

**Current SDK versions (from go.mod)**:
- `grafana-app-sdk`: v0.48.1 (direct require)
- `grafana-plugin-sdk-go`: v0.275.0 (indirect require)

**Usage scope (potentially impacted paths)**:
- `internal/common/client.go`
- `internal/resources/appplatform/`
- `pkg/provider/configure_clients.go`

## Technical Context

<!--
  ACTION REQUIRED: Replace the content in this section with the technical details
  for the project. The structure here is presented in advisory capacity to guide
  the iteration process.
-->

**Language/Version**: Go 1.25.3  
**Primary Dependencies**: Terraform Plugin SDK v2、Terraform Plugin Framework、Grafana App SDK、Grafana Plugin SDK Go、tfplugindocs  
**Storage**: N/A（Terraform Provider）  
**Testing**: `go test`、`make testacc*`、`golangci-lint`  
**Target Platform**: Terraform Provider（跨平台，主要运行在 Linux/CI）
**Project Type**: 单一 Go 模块仓库  
**Performance Goals**: N/A（依赖升级）  
**Constraints**: 禁止手工编辑 `docs/`；变更需可追踪且符合 SemVer 与发布流程  
**Scale/Scope**: 现有 Provider 代码库（多资源/数据源）

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

**Core Principles Compliance**:

- **Feature-Centric Development**: Feature list is the single source of truth; re-evaluate changes at every phase.
- **Generated Documentation**: Docs come from `tfplugindocs`/`go generate`; no manual edits in `docs/`.
- **Testing Mandate**: Acceptance tests for resources/data sources; unit tests for internal logic.
- **Terraform Standards**: Follow HashiCorp SDK/Framework best practices.
- **Go Tooling & Linting**: `go fmt`, `go vet`, `go generate`, `golangci-lint` before merge.
- **Release & Versioning**: SemVer compliance and GoReleaser/GitHub Actions release flow.

**Gates Status**: ✅ All gates pass

## Project Structure

### Documentation (this spec)

```text
.specify/specs/002-update-grafana-sdks/
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output (/speckit.plan command)
├── data-model.md        # Phase 1 output (/speckit.plan command)
├── quickstart.md        # Phase 1 output (/speckit.plan command)
├── contracts/           # Phase 1 output (/speckit.plan command)
├── feature-ref.md       # Phase 1 output (/speckit.plan command)
└── tasks.md             # Phase 2 output (/speckit.tasks command - NOT created by /speckit.plan)
```

### Source Code (repository root)
<!--
  ACTION REQUIRED: Replace the placeholder tree below with the concrete layout
  for this spec. Delete unused options and expand the chosen structure with
  real paths (e.g., apps/admin, packages/something). The delivered plan must
  not include Option labels.
-->

```text
cmd/
  generate/
  without-lister/
internal/
  common/
  functions/
  resources/
  testutils/
  util/
pkg/
  generate/
  provider/
templates/
examples/
docs/                 # Generated by tfplugindocs
scripts/
tools/
main.go
go.mod
```

**Structure Decision**: 单一 Go 模块仓库结构；依赖升级主要影响 go.mod/go.sum 与相关内部包引用。

## Complexity Tracking

N/A

## Implementation Notes & Validation Summary

- 依赖升级已应用至 go.mod/go.sum。
- 标准构建：`go build ./...` 通过。
- 关键使用场景验证：`go test ./internal/...` 通过（首次因 `/tmp` noexec 导致权限错误，使用 `GOTMPDIR` 重试成功）。
- 未手工编辑 `docs/`，保持生成文档约束。
